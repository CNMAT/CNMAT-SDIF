#ifndef SDIF_BOOLEANtypedef unsigned char Boolean;#undef TRUE#define TRUE 1#undef FALSE#define FALSE 0#define SDIF_BOOLEAN#endif//  required method codes inside each unit testtypedef enum{  ESDIF_UNIT_CMD_NAME,        //  returns name of test (for menu)  ESDIF_UNIT_CMD_RUN,         //  run the test (returns SDIFresult)  ESDIF_UNIT_CMD_END_OF_LIST} SDIFunitCmd;//  type of a generic unit testtypedef void *(UnitTestFn)(SDIFunitCmd cmd, ...);//  log levelstypedef enum{  LOG_PROMPT = 10,  LOG_ERROR = 10,  LOG_ASSERT = 6,  LOG_RESULTS = 5,  LOG_INFO = 3,  LOG_INFO2 = 2,  LOG_ALL = 0} SDIFunitLogLevel;//  prototypes for unit test shell functionsvoid unit_log(SDIFunitLogLevel level, char *fmt, ...);void unit_assert(Boolean test, char *fmt, ...);int unit_assert_GetCount();void unit_assert_SetCount(int count);